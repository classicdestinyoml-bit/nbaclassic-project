This is the NbaClassic React + Node.js Gift Card Marketplace project.
To run backend: cd backend && npm install && npm run dev
To run frontend: cd frontend && npm install && npm run dev
Configure .env files before running.
